# Backend origin. Leave empty in Vite dev to use the local proxy (see vite.config.js).
# Example for a deployed frontend: http://localhost:3000
VITE_API_URL=

# Seed employee used as submittedBy until authentication exists.
VITE_SUBMITTED_BY=user-employee-1
